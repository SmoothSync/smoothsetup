<resources>

    <!-- Wizard titles - all these strings have to fit on a single title line in an large font, so they should not exceed ~16 characters -->

    <!--
    Title of the wizard step that's shown while the app loads data from the SmoothSync service.
    Should not be longer than ~16 characters, since it has to fit on a single (title) line.
    -->
    <string name="smoothsetup_wizard_title_loading">Loading …</string>

    <!--
    Title of the wizard step that asks the user to enter his/her user name.
    Should not be longer than ~16 characters, since it has to fit on a single (title) line.
    -->
    <string name="smoothsetup_wizard_title_login">Log in</string>

    <!--
    Title of the wizard step that asks the user to enter his/her user name when logging in to a specific provider.
    Should not be longer than ~16 characters, since it has to fit on a single (title) line.
    -->
    <string name="smoothsetup_wizard_title_provider_login">%s</string>

    <!--
    Title of the manual setup page.
    -->
    <string name="smoothsync_manual_setup">Manual Setup</string>

    <!--
    Title of the wizard step that asks the user to select a provider.
    Should not be longer than ~16 characters, since it has to fit on a single (title) line.
    -->
    <string name="smoothsetup_wizard_title_choose_provider">Choose Provider</string>

    <!--
    Title of the wizard step that asks the user to enter his/her password.
    Should not be longer than ~16 characters, since it has to fit on a single (title) line.
    -->
    <string name="smoothsetup_wizard_title_enter_password">Enter password</string>

    <!--
    Title of the wizard step that's shown while the credentials verification is in progress.
    Should not be much longer than ~16 characters, since it has to fit on a single (title) line.
    -->
    <string name="smoothsetup_wizard_title_authenticating">Authenticating …</string>

    <!--
    Title of the wizard step that's shown while the app prepares the account on the device.
    Should not be much longer than ~16 characters, since it has to fit on a single (title) line.
    -->
    <string name="smoothsetup_wizard_title_completing_setup">Finishing …</string>

    <!--
    Title of the wizard step that's shown after the account setup has been completed.
    Should not be much longer than ~16 characters, since it has to fit on a single (title) line.
    -->
    <string name="smoothsetup_wizard_title_setup_completed">Congratulations</string>

    <!--
    Title of a wizard step that presents an error to the user.
    Should not be much longer than ~16 characters, since it has to fit on a single (title) line.
    -->
    <string name="smoothsetup_wizard_title_error">Error</string>

    <!--
    Title of a wizard step that presents an authentication error to the user.
    Should not be much longer than ~16 characters, since it has to fit on a single (title) line.
    -->
    <string name="smoothsetup_wizard_title_authentication_error">Authentication Error</string>


    <!--
    Title of a wizard step which asks the use to grant required permissions.
    -->
    <plurals name="smoothsetup_wizard_title_grant_permissions">
        <item quantity="one">Grant permission</item>
        <item quantity="two">Grant permissions</item>
        <item quantity="few">Grant permissions</item>
        <item quantity="many">Grant permissions</item>
        <item quantity="other">Grant permissions</item>
    </plurals>

    <!-- Prompts -->

    <!--
    Message that prompts the user to enter his/her username or email address for the provider to set up
    and to select the provider afterwards.
    -->
    <string name="smoothsetup_prompt_login">Enter your email address or login and choose your provider</string>

    <!--
    Message that prompts the user to enter provider and account data
    -->
    <string name="smoothsetup_enter_your_provider_data">Enter your provider data</string>

    <!--
    Message that prompts the user to enter his/her username or email address for a specific provider.
    %1$s will be replaced with the provider name.
    -->
    <string name="smoothsetup_prompt_login_at_provider">Enter your %1$s login</string>

    <!--
    Message that prompts the user to enter his/her password for a specific provider.
    %1$s will be replaced with the provider name.
    -->
    <string name="smoothsetup_prompt_enter_password">Enter your %1$s password</string>

    <!--
    Message that prompts the user to enter his/her password or app-specific password for a specific provider.
    %1$s will be replaced with the provider name.
    -->
    <string name="smoothsetup_prompt_enter_password_or_app_specific_password">Enter your %1$s password or your app-specific %1$s password</string>

    <!--
    Message that prompts the user to enter his/her app-specific password for a specific provider.
    %1$s will be replaced with the provider name.
    -->
    <string name="smoothsetup_prompt_enter_app_specific_password">Enter your app-specific %1$s password</string>


    <!--
    Message which prompts the user to grant one or more permissions.
    %1$s will be replaced with the app name
    -->
    <plurals name="smoothsetup_prompt_grant_permission">
        <item quantity="one">Please grant the following permission</item>
        <item quantity="two">Please grant the following permissions</item>
        <item quantity="few">Please grant the following permissions</item>
        <item quantity="many">Please grant the following permissions</item>
        <item quantity="other">Please grant the following permissions</item>
    </plurals>


    <!-- Buttons -->

    <!--
    Text on a button that allows the user to select a provider from a list.
    Shown if no other provider matches the login name.
    -->
    <string name="smoothsetup_button_choose_provider">Choose provider</string>


    <!--
    Text on a button that allows the user to launch the custom account setup.
    -->
    <string name="smoothsetup_button_manual_setup">Manual setup</string>

    <!--
    Text on a button that allows the user to select a provider from a list.
    Shown if the login name matches a specific provider.
    -->
    <string name="smoothsetup_button_other_provider">Other</string>

    <!--
    Text on a button that proceeds to the password prompt after entering the login name.
    -->
    <string name="smoothsetup_button_login_next">Next</string>

    <!--
    Text on a button that starts the credentials verification after entering a password.
    -->
    <string name="smoothsetup_button_sign_in">Sign in</string>

    <!--
    Text on the button under an error page that takes the user back to the previous step.
    -->
    <string name="smoothsetup_button_retry_now">Retry</string>

    <!--
    Text on button to grant permissions
    -->
    <string name="smoothsetup_button_grant_permission">Grant</string>

    <!--
    Text on a button to close the activity after a successful account setup.
    -->
    <string name="smoothsetup_button_close">Close</string>

    <!-- Messages -->

    <!--
    Message that's shown to the user when everything worked out an the setup has been completed successfully.
    %1$s will be replaced with the app name.
    -->
    <string name="smoothsetup_message_setup_completed">%1$s is now set up and synchronization will start shortly</string>

    <!--
    Message that's shown to the user his/her credentials haven been validated successfully.
    %1$s will be replaced with the app name.
    -->
    <string name="smoothsetup_message_auth_completed">%1$s has successfully validated your account and synchronization will continue shortly</string>

    <!--
    Message that's faded in if a blocking operation (usually a network operation) takes longer than a few seconds.
    -->
    <string name="smoothsetup_message_please_wait">This can take a moment.</string>

    <!--
    Message presented to the user when an authentication error occurred.
    %1$s will be replaced with the app name
    %2$s will be replaced with the user name
    %3$s will be replaced with the provider name.
    -->
    <string name="smoothsetup_message_authentication_failure">%1$s could not authenticate your account \"%2$s\" with %3$s.\n\nPlease sign in to authorize %1$s to sync your account.</string>


    <!-- Error Messages -->

    <!--
    Error message that's shown when no provider data (for one or multiple providers) could be loaded (due to any kind of error).
    -->
    <string name="smoothsetup_error_load_provider">Couldn\'t load provider data.\n\nPlease check your internet connection or retry later.</string>

    <!--
    Error message that's shown when the user's credentials were not accepted by the provider.
    -->
    <string name="smoothsetup_error_authentication">Authentication failed.\n\nPlease verify your password and retry.</string>

    <!--
    Generic network error message that's shown when there was any sort of connection issue.
    -->
    <string name="smoothsetup_error_network">Connection failed.\n\nPlease check your internet connection or retry later.</string>

    <!--
    Generic network error message that's shown when there any other issue.
    -->
    <string name="smoothsetup_error_other">Unknown Error: "%s"</string>

    <!--
    Error message shown when the user entered a host name that can't be resolved.
    -->
    <string name="smoothsetup_error_unknown_host">Host not found.</string>

    <!--
    Error message shown when the user entered a host name that can't be resolved.
    -->
    <string name="smoothsetup_error_invalid_url">Please enter a valid hostname or https URL.</string>

    <!--
    Error message shown when the user entered an URL without https scheme.
    -->
    <string name="smoothsetup_error_insecure_scheme">Please use the https scheme.</string>

    <!-- EditText hints -->

    <!--
    Hint text in the field that takes the user's email address or login name.
    -->
    <string name="smoothsetup_default_login_hint">Email address or login</string>

    <!--
    Hint text in the field which takes the server address or name.
    -->
    <string name="smoothsetup_hostname_or_address">Hostname or address</string>

    <!--
    Hint text in the field that takes the user's login name.
    -->
    <string name="smoothsetup_manual_login_hint">Username</string>

    <!--
    Hint text on a password input field.
    -->
    <string name="smoothsetup_hint_password">Password</string>


    <!-- Links -->

    <!--
    Text of a link that will be shown on the login screen if a service provides a sign up link.
    -->
    <string name="smoothsetup_sign_up_now">No account yet? <b>Sign up now.</b></string>

    <!--
    Text of a link that will be shown underneath the password dialog if a service provides a password recovery link.
    -->
    <string name="smoothsetup_forgot_password">Forgot your password?</string>

    <!--
    Text of a link that will be shown underneath the password dialog if a service provides a link to create app specific passwords.
    -->
    <string name="smoothsetup_create_app_specific_password">Create app-specific password</string>

    <!--
    Text of a link that opens a privacy policy when clicked.
    %1$s wil be replaced either with the app name or the provider name.
    -->
    <string name="smoothsetup_privacy_policy">%1$s Privacy Policy</string>

    <!--
    Text of a link that opens terms & conditions when clicked.
    %1$s wil be replaced either with the app name or the provider name.
    -->
    <string name="smoothsetup_terms_and_conditions">%1$s Terms and Conditions</string>


    <!-- App specific password helper -->

    <!--
    Title of a button on the Snackbar to copy an app specific password to the clipboard.
    Must be a single word, as short as possible. Will be converted to all-caps by Android.
    -->
    <string name="smoothsetup_button_copy_app_specific_password">Copy</string>

    <!--
    Title of a Snackbar message to inform the user that an app-specific password has been found.
    %1$s will be replaced by the password that was found.
    -->
    <string name="smoothsetup_snackbar_found_app_specific_password">App-specific password found: %1$s</string>

    <!--
    Title of a Snackbar message to inform the user that the previously found app-specific password has been inserted into the password field.
    -->
    <string name="smoothsetup_app_specific_password_inserted">App-specific password inserted</string>


</resources>
